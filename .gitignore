# Internal marketing workspace — NOT committed to the public repo.
# Lives locally as Cowork's working area only.
BRAND.md
MESSAGING.md
PRICING.md
CAST.md
CLAUDE.md
COPY/
posts/
outreach/

# Local staging area before content is moved into docs/
site/

# Operational source-of-truth — privacy-sensitive (bank statements, PII emails,
# tax docs, workbooks). MUST NEVER be committed to this PUBLIC repo (Hard Rule #1).
# The real admin/ lives at the workspace level (../admin); this guard also catches
# any stray copy accidentally created inside the repo.
admin/

# OS / editor
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# Future build artefacts (if Astro/Next ever lives here)
node_modules/
.next/
.astro/
dist/
build/
